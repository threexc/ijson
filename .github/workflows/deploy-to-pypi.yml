name: Build distributions and upload to PyPI

# Build on every branch push, tag push, and pull request change:
on:
  push:
  pull_request:
  schedule:
  # 00:00 UTC every Saturday, don't bother anyone
  - cron: '0 0 * * 6'

env:
  UV_EXTRA_INDEX_URL: https://pypi.riseproject.dev/simple/
  UV_INDEX_STRATEGY: unsafe-best-match

jobs:

  calculate_wheels_to_build:
    name: Calculate OS/archs to build wheels on
    runs-on: ubuntu-latest
    env:
      BUILD_TYPE: ${{ (startsWith(github.event.ref, 'refs/tags/v') || startsWith(github.event.ref, 'refs/heads/fullci_') || github.event.schedule == '0 0 * * 6') && 'FULL' || 'BASE' }}
      ARCHS_LINUX_INTEL_BASE: '["x86_64"]'
      ARCHS_LINUX_INTEL_FULL: '["x86_64", "i686"]'
      ARCHS_LINUX_ARM_BASE: '["aarch64"]'
      ARCHS_LINUX_ARM_FULL: '["aarch64"]'
      ARCHS_LINUX_RISCV_BASE: '["riscv64"]'
      ARCHS_LINUX_RISCV_FULL: '["riscv64"]'
      ARCHS_MACOS_BASE: '["x86_64"]'
      ARCHS_MACOS_FULL: '["x86_64", "arm64", "universal2"]'
      ARCHS_WINDOWS_BASE: '["AMD64"]'
      ARCHS_WINDOWS_FULL: '["AMD64", "x86"]'
      ARCHS_WINDOWS_ARM_BASE: '["ARM64"]'
      ARCHS_WINDOWS_ARM_FULL: '["ARM64"]'
    outputs:
      matrix: ${{ steps.set-matrix.outputs.matrix }}
    steps:
      - name: Calculate strategy matrix
        shell: python
        id: set-matrix
        run: |
          import json
          import os

          combinations = (
              ("ubuntu-24.04", "ARCHS_LINUX_INTEL"),
              ("ubuntu-24.04-arm", "ARCHS_LINUX_ARM"),
              ("ubuntu-24.04-riscv", "ARCHS_LINUX_RISCV"),
              ("macos-14", "ARCHS_MACOS"),
              ("windows-2025", "ARCHS_WINDOWS"),
              ("windows-11-arm", "ARCHS_WINDOWS_ARM"),
          )
          build_type = os.getenv("BUILD_TYPE")
          includes = [
            {"os": os_name, "arch": arch}
            for os_name, archs_envvar in combinations
            for arch in json.loads(os.getenv(f'{archs_envvar}_{build_type}'))
          ]
          matrix = {"include": includes}
          with open(os.getenv("GITHUB_OUTPUT"), "at") as github_output:
            github_output.write(f'matrix={json.dumps(matrix)}')
          print(f"Calculated matrix strategy:\n{json.dumps(matrix, indent=2)}")

  build_wheels:
    name: Build wheels for ${{ matrix.os }} / ${{matrix.arch}}
    needs: calculate_wheels_to_build
    runs-on: ${{ matrix.os }}
    strategy:
      matrix: ${{ fromJson(needs.calculate_wheels_to_build.outputs.matrix) }}
    env:
      CIBW_ARCHS: ${{ matrix.arch }}
      CIBW_BUILD_FRONTEND: "build[uv]"

    steps:
      - uses: actions/checkout@v7
        with:
          submodules: true

      - name: Build wheels
        uses: pypa/cibuildwheel@v4.2.1
        with:
          output-dir: wheelhouse
          # CIBW_BUILD_FRONTEND uses uv; without this the action never
          # installs its own uv and just hopes one is already on PATH
          # (true on some runner images, not on macos-14 -> "uv not found").
          extras: uv

      - uses: actions/upload-artifact@v7
        with:
          name: wheels-${{ matrix.os }}-${{ matrix.arch }}
          path: ./wheelhouse/*.whl

  build_sdist:
    name: Build source distribution
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Python ${{ matrix.config.python }}
        uses: astral-sh/setup-uv@20cfd1bf945f4377ade1205e4dbc17946fc9a30d # v10.0.1
        with:
          activate-environment: true
          python-version: '3.13'

      - name: Install build
        run: uv pip install build

      - name: Build sdist
        run: uv build --sdist

      - uses: actions/upload-artifact@v7
        with:
          name: source-dist
          path: dist/*.tar.gz

  test_sdist:
    name: Check source distribution is usable
    needs: [build_sdist]
    runs-on: ubuntu-latest
    steps:
      - name: Python ${{ matrix.config.python }}
        uses: astral-sh/setup-uv@20cfd1bf945f4377ade1205e4dbc17946fc9a30d # v10.0.1
        with:
          activate-environment: true
          python-version: '3.13'

      - uses: actions/download-artifact@v8
        with:
          name: source-dist

      - name: Extract source distribution
        run: tar xvf ijson*.tar.gz && rm ijson*.tar.gz

      - name: Install source distribution
        run: uv pip install ./ijson*

      - name: Install test dependencies
        run: uv pip install pytest cffi

      - name: Run source distribution tests
        run: cd ijson* && pytest

  merge_artifacts:
    needs: [build_wheels, build_sdist]
    runs-on: ubuntu-latest
    steps:
      - name: Merge Artifacts
        uses: actions/upload-artifact/merge@v7
        with:
          name: all-artifacts

  upload_pypi:
    needs: [merge_artifacts]
    runs-on: ubuntu-latest
    if: startsWith(github.event.ref, 'refs/tags/v')
    steps:
      - uses: actions/download-artifact@v8
        with:
          name: all-artifacts
          path: dist

      - uses: pypa/gh-action-pypi-publish@master
        with:
          user: __token__
          password: ${{ secrets.pypi_password }}
